services:
  Tweetcord:
    build: .
    image: ghcr.io/pokedo0/tweetcord:latest
    pull_policy: always
    container_name: tweetcord
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data/:/bot/data
      - ./configs.yml:/bot/configs.yml